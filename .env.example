# Non-secret deployment identity.
ATHARVAN_ENVIRONMENT=development
ATHARVAN_PUBLIC_ORIGIN=http://localhost:3000

# Secret values are configured through the runtime secret store.
DATABASE_URL=
BETTER_AUTH_SECRET=
ATHARVAN_VERIFICATION_HMAC_SECRET=
ATHARVAN_SUPER_ADMIN_EMAIL=

# Transactional email is provider-neutral. Provider-specific names are added
# only when the first adapter is selected.
ATHARVAN_EMAIL_FROM=
