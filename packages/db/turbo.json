{
  "extends": ["//"],
  "tasks": {
    "build": { "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/scripts/**/*.ts"] },
    "typecheck": {
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/scripts/**/*.ts"]
    },
    "test": { "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/scripts/**/*.ts"] }
  }
}
